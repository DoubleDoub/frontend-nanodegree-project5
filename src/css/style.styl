@import "mixins.styl"
// @import "sprite-navigation-white.css"


// variables
//z-index 'namespacing'
$z-app  = 1000
$z-notification = 9000

// reset box-sizing
html {
    -webkit-box-sizing border-box
    -moz-box-sizing border-box
    box-sizing border-box
}

*, *:before, *:after {
    -webkit-box-sizing inherit
    -moz-box-sizing inherit
    box-sizing inherit
}

body, html {
    margin 0
    padding 0
    background-color beige
    height 100%
    //full-screen()
    overflow hidden
}

ul {
    margin 0
    padding 0
}

nav {
    positon absolute
    top 0
    //transform translateX(-300px)
    transition transform 0.2s
    //width 300px
    margin 0
    padding 0
    background-color green
}

nav.open {
    transform translateX(0px);
}


.search-bar { 
    width 300px
}

.app {
    full-screen()
    height 100%
    z-index z-app
}
.flex-container {
    flex-container(column, stretch, flex-start, wrap)
}

header {
    height 10%
    background-color red
}

main {
    flex-container(row, stretch, flex-start, wrap)
    height 80%


}
.map {
    height 100%
}

footer {
    height 10%
    background-color lime
}

.list {
    overflow auto
}

@media screen and (max-width : 499px) {
    .list {
        width 0%
        //transform translateX(-499px)
        transition translate, width 0.5s ease
    }
    .list.list-active {
        width 100%
    }
    .map {
        width 100%
        // tranform translateX (499px)
        transition translate, width 0.5s ease
        
    }
    .map.list-active {
        width 0%
        // transform translateX(0px)
    }
}

@media screen and (min-width : 500px) {
    main {
        flex-container(row, stretch, flex-start, wrap)
    }
    
    .list {
        width 50%
    }
    .map {
        width 50%
    }
}

.hamburger-menu
{
    width:30px;
    height:5px;
    background-color:#111;
    border-radius:5px;
    position:relative;
}

.hamburger-menu:after, .hamburger-menu:before
{
    content: '';
    width: 100%;
    height:5px;
    background-color:#111;
    position:absolute;
    border-radius:5px;
}

.hamburger-menu:after
{
    top:10px;
}

.hamburger-menu:before
{
    top:20px;
}

$notif-width = 50%
.notification {
    //display none
    position absolute
    top $notif-width
    width $notif-width
    left $notif-width
    transform translateX(100%)
    z-index $z-notification
    background-color red
    transition transform 0.2s
    overflow hidden
}

.showNotification {
    transform translateX(-50%)
}

.icon {
    display inline-block
}

.savedGuide {
    margin-right 1rem
    margin-left 1rem
    border-bottom-style solid
    border-color #bdbdbd
}

.guideControl {
    //margin auto auto
}